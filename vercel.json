{
    "version": 2,
    "builds": [
        {
            "src": "server.js",
            "use": "@vercel/node"
        },
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "config": {
                "distDir": "public/build"
            }
        }
    ],
    "routes": [
        {
            "src": "/api/(.*)",
            "dest": "server.js"
        },
        {
            "src": "/(.*)",
            "dest": "/public/build/$1"
        }
    ],
    "env": {
        "NODE_ENV": "production",
        "JWT_EXPIRE": "30d"
    },
    "build": {
        "env": {
            "VITE_APP_NAME": "JetSet App",
            "VITE_API_URL": "/api"
        }
    }
}