# Node.js Backend Environment Variables

# Server Configuration
PORT=5000
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/jetset
# For production, use MongoDB Atlas URI
# MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/jetset

# JWT Configuration
JWT_SECRET=jetset-app-secret-key-change-in-production
JWT_EXPIRE=30d

# CORS Configuration
CORS_ORIGIN=http://localhost:5173

# Keep any Laravel settings you still need below
